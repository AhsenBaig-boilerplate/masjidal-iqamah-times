<<<<<<< HEAD
=======

>>>>>>> dev
name: Deploy Dev
  name: Deploy Dev

        uses: actions/deploy-pages@v4
    push:
      branches: [ dev ]
    workflow_dispatch:

  permissions:
    contents: read
    pages: write
    id-token: write

        with:
    build:
      runs-on: ubuntu-latest
      env:
        FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
      steps:
        - uses: actions/checkout@v4
        # Write the correct CNAME for dev environment
        - name: Set CNAME for dev
          run: echo "dev.masjidal-iqamah-times.ahsenbaig.dev" > CNAME
        # Removed npm ci and npm run build:dev steps (not a Node.js project)
        - name: Upload artifact
          uses: actions/upload-pages-artifact@v3
          with:
            path: .

    deploy:
      needs: build
      runs-on: ubuntu-latest
      environment:
        name: dev
        url: https://dev.masjidal-iqamah-times.ahsenbaig.dev
      steps:
        - name: Deploy to GitHub Pages
          uses: actions/deploy-pages@v4
          cname: dev.blog.example.com
